@page
@model GroceryListManager.Pages.GroceryList_.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>GroceryList</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="GroceryList.Id" />
            <div class="form-group">
                <label asp-for="GroceryList.Name" class="control-label"></label>
                <input asp-for="GroceryList.Name" class="form-control" />
                <span asp-validation-for="GroceryList.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="GroceryList.Price" class="control-label"></label>
                <input asp-for="GroceryList.Price" class="form-control" />
                <span asp-validation-for="GroceryList.Price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="GroceryList.PurchasedDate" class="control-label"></label>
                <input asp-for="GroceryList.PurchasedDate" class="form-control" />
                <span asp-validation-for="GroceryList.PurchasedDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
